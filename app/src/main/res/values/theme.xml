<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--Base Themes-->

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorButtonNormal">@color/accent</item>
        <item name="colorControlNormal">@color/accent</item>
        <item name="colorControlActivated">@color/accent_dark</item>
        <item name="colorControlHighlight">@color/accent_dark</item>
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="AppTheme.Base.DarkStatusBar" parent="AppTheme.Base"></style>

    <style name="AppTheme.Base.TransparentStatusBar" parent="AppTheme.Base.DarkStatusBar">
        <!--Won't work here-->
    </style>


    <style name="AppTheme.Base.Dark.TransparentStatusBar" parent="AppTheme.Base.Dark">
        <!--Won't work here-->
    </style>

    <style name="AppTheme.DarkStatusBar" parent="AppTheme.Base.DarkStatusBar"></style>

    <style name="AppTheme.Base.Dark" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/dark_primary_bright</item>
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorPrimaryDark">@color/dark_primary_dark</item>

        <item name="colorAccent">@color/accent</item>
        <item name="colorButtonNormal">@color/accent</item>
        <item name="colorControlNormal">@color/accent</item>
        <item name="colorControlActivated">@color/accent_dark</item>
        <item name="colorControlHighlight">@color/accent_dark</item>

        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <!--Final Themes-->

    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:selectableItemBackground">?attr/selectableItemBackground</item>
    </style>

    <style name="AppTheme.Dark" parent="AppTheme.Base.Dark">
        <item name="android:selectableItemBackground">?attr/selectableItemBackground</item>
    </style>

    <style name="AppTheme.TransparentStatusBar" parent="AppTheme.Base.TransparentStatusBar">
        <item name="android:selectableItemBackground">?attr/selectableItemBackground</item>
    </style>

    <style name="AppTheme.Dark.TransparentStatusBar" parent="AppTheme.Base.Dark.TransparentStatusBar">
        <item name="android:selectableItemBackground">?attr/selectableItemBackground</item>
    </style>

    <!--Toolbar-->

    <style name="Toolbar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="actionOverflowButtonStyle">@style/Widget.ActionButton.Overflow</item>
        <item name="android:actionOverflowButtonStyle">@style/Widget.ActionButton.Overflow</item>
    </style>

    <style name="Toolbar.Dark" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="actionOverflowButtonStyle">@style/Widget.ActionButton.Overflow</item>
        <item name="android:actionOverflowButtonStyle">@style/Widget.ActionButton.Overflow</item>
    </style>

    <style name="Widget.ActionButton.Overflow" parent="Base.Widget.AppCompat.ActionButton.Overflow">
        <item name="android:contentDescription">@string/accessibility_overflow</item>
    </style>


    <style name="AppTheme.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/white_primary</item>
    </style>

    <style name="AppTheme.Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!-- Set proper title size -->
        <item name="android:textSize">@dimen/abc_text_size_title_material_toolbar</item>
        <item name="android:textColor">@color/white_primary</item>
        <item name="android:textColorPrimary">@color/white_primary</item>
    </style>

    <style name="AppTheme.Toolbar.Title.Dimmed" parent="AppTheme.Toolbar.Title">
        <item name="android:textColorPrimary">@color/white_hint</item>
        <item name="android:textColor">@color/white_hint</item>
    </style>
</resources>